<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nuc.edu.lumecho.mapper.PhotographyActivityMapper">
    <update id="cancelActivity">
        UPDATE photography_activity
        SET status = 3,
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <select id="selectPendingActivity"
            resultType="nuc.edu.lumecho.model.entity.PhotographyActivity">

        SELECT a.*,
               COUNT(ap.id) AS currentParticipants
        FROM photography_activity a
                 LEFT JOIN activity_application ap
                           ON a.id = ap.activity_id AND ap.status = 1
        WHERE a.status = 0
        GROUP BY a.id
        ORDER BY a.create_time DESC

    </select>

</mapper>