<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nuc.edu.lumecho.mapper.ChallengeMapper">
    <select id="selectChallengeList" resultType="nuc.edu.lumecho.model.entity.Challenge">
        SELECT
        id, title, description, start_time, end_time, review_end_time,
        status, max_participants, participant_count, created_at
        FROM challenge
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>
</mapper>